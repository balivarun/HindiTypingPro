# Copy this to .env.local for local development (already handled by Vite proxy)
# Required on Vercel — set this in Project Settings → Environment Variables
VITE_API_URL=https://your-backend-name.onrender.com
